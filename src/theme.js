import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme(
    /** Here you can override default theme values */
    {
        spacing: 8,
    },
    /** Here you can add your custom values */
    {},
);

export default theme;
